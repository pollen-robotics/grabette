# UI + Hub deps for the Space. The grabette-postprocess package (and its deps:
# lerobot, numpy, opencv, pandas, ...) is installed from source in the Dockerfile.
# gradio[oauth] pulls itsdangerous/authlib, required by gr.LoginButton (HF OAuth).
gradio[oauth]>=4.0
huggingface_hub>=0.20
# `spaces` satisfies Gradio's in-Space hot-reload watcher (SYSTEM=spaces) so it
# doesn't raise in a background thread.
spaces
